{
  "name": "google-calendar-duplicator",
  "description": "",
  "dependencies": {
    "@types/google-apps-script": "^2.0.11"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/node": "^25.9.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "lint": "biome check",
    "fix": "biome check --write",
    "format": "biome format --write",
    "clean": "rm -rf build",
    "compile": "tsc",
    "prepare": "pnpm run compile",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": "24.x"
  }
}
